# deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: neighborlyapp
spec:
  selector: # Define the wrapping strategy
    matchLabels: # Match all pods with the defined labels
      app: neighborlyapp # Labels follow the `name: value` template
  template: # This is the template of the pod inside the deployment
    metadata:
      labels:
        app: neighborlyapp
    spec:
      containers:
        - image: hlodreg.azurecr.io/neighborlyapp
          name: neighborlyapp
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          ports:
            - containerPort: 5000
              name: http
          imagePullSecrets:
          - name: all-icr-io